# Vercel Git connection check

Temporary, branch-only marker used to verify that a GitHub push creates a
Vercel Preview deployment after the Git integration was reauthorized on
2026-08-18. This file must not be merged into `main`.

import { getCatalogProductMetadata } from '../catalogProductMetadata'

export function getSnapchatCatalogProductMetadata(
  productHandle: string
) {
  return getCatalogProductMetadata(productHandle)
}
